#include "main.h"

/**
* _abs - return absolute value
* @n: The character to check absolute
* Return: return -n if negative else n
*/

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
